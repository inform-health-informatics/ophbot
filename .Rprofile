# source("renv/activate.R")
setwd('dev')
print('*** All dev work will be in ./dev')
print('*** Only ./dev/app.R and files in ./dev/app/ will be made available to the production container ')
print("*** Call renv::snapshot(lockfile = '~/dev/renv.lock'), packages=.packages()) when you finish to capture the packages for docker")
getwd()

print("*** IF WORKING FROM UCLH DS DESKTOP then you will need to push code to github then pull back to GAE")
print("*** THEN use make app-buildso to copy work from dev/app to ./app (i.e. root of docker container)")
